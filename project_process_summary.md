# AI 기안/품의서 초안 생성 도우미 프로세스 정의서

본 문서는 그룹웨어 전자결재 연동 'AI 기안/품의서 초안 생성기' 프로젝트의 전체적인 구동 방식과 주요 프로세스, 그리고 설계 및 AI API 활용 시 주의사항을 종합적으로 정리한 가이드입니다.

> 💡 **인수인계 관련 상세 문서 안내:**
> - 인수인계 종합 마스터 가이드: [HANDOVER.md](file:///c:/Users/ycm46/OneDrive/Desktop/AI/gw_ai/HANDOVER.md)
> - 시스템 아키텍처 및 상세 설계서: [docs/01_system_architecture_and_design.md](file:///c:/Users/ycm46/OneDrive/Desktop/AI/gw_ai/docs/01_system_architecture_and_design.md)
> - AI 파이프라인 및 프롬프트 가이드: [docs/02_ai_pipeline_and_prompts.md](file:///c:/Users/ycm46/OneDrive/Desktop/AI/gw_ai/docs/02_ai_pipeline_and_prompts.md)
> - 운영, 배포 및 트러블슈팅 가이드: [docs/03_operation_and_troubleshooting.md](file:///c:/Users/ycm46/OneDrive/Desktop/AI/gw_ai/docs/03_operation_and_troubleshooting.md)
> - 이슈 해결 히스토리: [issue_and_resolution_history.md](file:///c:/Users/ycm46/OneDrive/Desktop/AI/gw_ai/issue_and_resolution_history.md)
> - 향후 개선 과제: [future_improvements.md](file:///c:/Users/ycm46/OneDrive/Desktop/AI/gw_ai/future_improvements.md)

---

## 1. 전체 프로그램 구동 아키텍처 및 흐름 (Overall Architecture & Flow)

본 시스템은 크게 두 가지 핵심 모듈로 구성되어 동작합니다.
1. **과거 데이터 구축 및 전처리 배치 프로세서 (`gw_ai.py`)**: RAG(검색 증강 생성)에 활용될 사내 지식 데이터베이스 구축
2. **실시간 기안/품의서 초안 생성 어플리케이션 (`app.py`)**: 사용자와 상호작용하며 실제 문서를 생성하는 웹 앱

---

## 2. 모듈별 상세 프로세스

### Part 1. 과거 결재 문서 배치 전처리 프로세스 (`gw_ai.py`)
RAG 검색 품질을 높이기 위해 원본 DB(A)의 과거 결재 문서를 전처리하여 AI 전용 DB(B)에 적재하는 프로세스입니다.
1. **서버 연결 및 초기화**: 원본 DB(A)와 AI 전용 DB(B)에 접속하여 타겟 테이블을 확인합니다.
2. **일자별 배치 데이터 수집**: `START_DATE` 부터 `END_DATE` 까지 날짜별로 쿼리를 수행하여 문서를 가져옵니다. (중복 제외)
3. **AI 전처리 및 변환 (Batch 단위 처리)**:
   - 본문 HTML을 마크다운(Markdown)으로 변환합니다.
   - `processor.py` (Gemini API)를 통해 **핵심 키워드(Keywords)와 요약(Summary)**을 추출합니다.
4. **저장**: 추출된 메타데이터(키워드, 요약)와 함께 DB(B)에 적재하여, 추후 유사 문서 검색 시 활용될 수 있게 합니다.

### Part 2. 실시간 AI 기안 초안 생성 프로세스 (`app.py`)
사용자가 웹 인터페이스를 통해 빠르고 정확하게 초안을 작성할 수 있도록 돕는 4단계 파이프라인입니다.

#### STEP 1. 기본 정보 수집 및 의도 파악
- **정보 입력**: 사용자 사번, 결재 양식(기안, 품의, 휴가 등), 작성 목적 및 요약 텍스트를 입력받습니다.
- **유사 문서 검색 (전환기)**: 입력된 요약을 바탕으로 사용자가 권한을 가진 DB(B)의 과거 문서를 1차로 조회하고, LLM과 Cohere Reranker를 통해 **상위 3개의 가장 유사도 높은 참고 문서를 선별**합니다.

#### STEP 2. 참고 문서 검토 및 첨부파일 처리
- **문서 확인**: 선별된 과거 유사 문서 3건을 사용자가 확인하고, 초안 작성에 반영할 문서를 취사선택합니다.
- **첨부파일 전처리 및 비동기 업로드 (`processor.py`)**:
  - 병렬 처리(`ThreadPoolExecutor`)로 사용자가 업로드한 첨부파일(PDF, 엑셀 등)을 구글 서버에 업로드합니다.
  - **(중요 원칙) 엑셀 파일 마크다운 변환**: 빈 행과 열을 `pandas`를 통해 깨끗하게 제거한 뒤, 마크다운 표(Markdown Table) 형식으로 변환하여 업로드합니다. (AI의 숫자/표 인식률 극대화를 위한 **핵심 보존 로직**)

#### STEP 3. 팩트 체크 및 필수 체크리스트 점검
- **핵심 팩트(Fact) 사전 추출**: LLM의 환각(Hallucination) 오류를 막기 위해, 첨부된 파일에서 품명, 단가, 수량, 총액 등의 팩트 데이터를 JSON 형태로 먼저 추출합니다.
- **스마트 체크리스트 도출**: 작성 예정 양식에 필수적으로 들어가야 할 5~7개의 항목을 AI가 도출합니다. 이미 첨부파일에서 파악된 내용은 자동 채움(Pre-fill)되고, 빈 항목은 사용자에게 입력을 유도하거나 비워둘 경우 문맥에 맞게 AI가 자동 작성(Auto-fill)합니다.

#### STEP 4. 최종 초안 생성 및 결과 튜닝
- **초안 생성**: 팩트 데이터, 체크리스트 답변, 과거 참고 문서의 서식(구조), 원본 첨부파일 내용 전체를 종합하여 최종 기안서 HTML을 생성합니다. (단락별 `<h2>, <h3>` 태그로 분리)
- **단락별 수정 (Refine via Chat)**: 생성된 초안을 단락(Section) 단위로 나누어, 사용자가 각 단락별로 추가 수정 지시(Comment)를 내릴 수 있습니다. 수정 시 병렬 처리를 통해 여러 단락을 동시에 재생성합니다.
- **DB 이력 추적**: 생성된 기안 초안 이력과 채팅 수정 로그(수정 전/후 데이터)를 영구 기록(`EAPPDocument_AI_Result`, `EAPPDocument_AI_ChatLog`)하여 추후 감사 및 모델 개선에 활용합니다.

---

## 3. 핵심 설계 원칙 및 주의사항

> [!IMPORTANT]
> **첨부파일 파싱 정확도 유지 (핵심 보존 규칙)**
> `processor.py` 내의 첨부파일 파싱 로직, 특히 **엑셀 파일을 읽어 빈 행/열을 제거(`dropna(how='all')`)하고 마크다운 표(Markdown Table)로 변환하는 로직은 높은 정확도를 달성**한 상태입니다. 시스템 성능에 치명적인 영향을 미칠 수 있으므로 **이 로직은 어떠한 경우에도 임의로 훼손되거나 변경되어서는 안 됩니다.** 만약 수정이 필요할 경우, 정확도 하락 위험에 대해 즉각적인 경고를 발생시키고 개발/관리자의 명시적인 확인을 거쳐야 합니다.

> [!CAUTION]
> **할루시네이션(환각) 방지 통제**
> LLM은 금액, 수량 등 수치 데이터를 마음대로 추정하려는 성향이 강합니다.
> - **초안 생성 및 수정**: 최초 초안 생성 시 주입된 팩트 데이터를 100% 반영하여 과거 문서 내용을 덮어씌웁니다. 단락별 수정 시에도 원본 팩트 데이터와 원본 첨부파일을 지속적으로 주입(Context Injection)하여 없는 내용을 지어내지 않도록 방지합니다.

> [!WARNING]
> **출력 포맷의 강제성 (순수 HTML)**
> 생성된 초안은 곧바로 그룹웨어 에디터에 붙여넣기(Ctrl+V) 되어야 하므로 마크다운 백틱(```html)이나 `<html>`, `<body>` 같은 외부 뼈대 태그가 섞이면 레이아웃이 깨집니다. 프롬프트 내에 **"순수 HTML 본문 태그만 반환할 것"**이라는 조건을 반드시 기재 및 유지해야 합니다.

---

## 4. 보안 및 환경 설정
- **권한 분리 검색**: 유사 문서 검색 시 실운영 DB와 연동할 때 철저히 `WriterID`나 `DepartID` 기준으로 조인하여 민감한 문서를 보호합니다.
- **자격 증명 분리**: DB 연결 정보 및 API Key 등은 `.env` 및 `config.py`를 통해 안전하게 분리되어 관리됩니다.� 넘겨 AI가 자동 작성(Auto-fill) 하도록 맡길 수 있습니다.

### STEP 4. 초안 생성 및 결과 저장 (`app.py` - step 4)
- **최종 초안 생성:**
  - 체크리스트 사용자 답변(또는 AI 오토필), 사전 추출된 팩트 데이터, 상위 3개 과거 문서 본문, 원본 첨부파일(PDF 등)을 모두 융합하여 기안을 생성합니다.
  - 금액과 수량은 팩트 데이터를 절대적으로 신뢰하되, **일정표(WBS)나 아키텍처 등 시각적인 세부 정보는 원본 첨부파일을 직접 판독**하여 본문(표 등)에 완벽하게 반영합니다.
- **초안 즉석 수정 (Refine via Chat) 및 컨텍스트 무한 리필:**
  - 화면 하단의 채팅창(`st.chat_input`)을 통해 초안 내용을 덮어쓰기(Overwrite) 형태로 계속 깎아나갈 수 있습니다. (`refine_draft`)
  - **(할루시네이션 차단):** "표를 요약해 줘" 이후 다시 "표를 상세히 풀어서 원상 복구해 줘"라고 요청할 때 AI가 상상력을 발휘하는 것을 막기 위해, 매 수정 채팅마다 **[원본 팩트 데이터]와 [원본 첨부파일]을 백그라운드에서 다시 주입(Context Injection)**합니다.
- **DB 이력 추적 및 감사 (Audit):**
  - 최초 초안 내역을 `EAPPDocument_AI_Result`에 저장할 뿐만 아니라, 사용자가 채팅으로 어떤 지시를 내려 어떻게 수정되었는지를 추적하기 위해 `EAPPDocument_AI_ChatLog` 테이블에 `(요청내용, 수정 전 초안, 수정 후 초안)` 이력을 완벽하게 영구 기록합니다.

---

## 3. 중요 체크사항

> [!IMPORTANT]
> **Streamlit 캐시 관리 (`@st.cache_resource`)**
> Streamlit은 `Processor` 같은 연결 객체를 전역으로 캐싱하여 메모리에 쥐고 있습니다. 내부 코드를 수정하여 로직을 변경했을 때는 반드시 캐시를 무효화(함수명 변경 등) 하거나 앱을 완전히 재기동해야 수정된 파싱 로직이 정상 반영됩니다.

> [!TIP]
> **데이터베이스 권한 및 보안**
> 2단계에서 실운영 그룹웨어 DB를 쿼리할 때, 철저히 `WriterID`나 `DepartID` 기준으로 JOIN 하여 타 부서의 민감한 기밀 문서가 타인에게 추천되지 않도록 주의해야 합니다.

---

## 4. AI API 호출 전 주요 주의사항 (프롬프트 엔지니어링)

> [!CAUTION]
> **할루시네이션(환각) 방지 통제**
> LLM은 금액, 수량 등 수치 데이터를 마음대로 추정하려는 성향이 강합니다.
> - **`generate_draft()` / `refine_draft_section()`:**
  - `generate_draft()`: 최초 초안 생성 시, 주입된 팩트 데이터를 100% 반영하여 과거 문서의 내용을 덮어씌움.
  - `split_html_into_sections()`: 생성된 HTML을 헤딩 태그 기준으로 쪼개는 헬퍼 함수.
  - `refine_draft_section()`: 사용자가 입력한 단락별 수정 지시를 바탕으로 해당 단락만 재생성함. 이 때 전체 초안 텍스트를 문맥으로 제공하여 앞뒤 논리가 깨지지 않도록 함.
> - **데이터 격리:** 사용자에게 받은 정보(A)와 과거 문서(B)를 섞을 때, 과거 문서(B)는 "스타일 및 구조" 용도로만 사용하도록 명확히 선을 그어주어야 합니다.

> [!WARNING]
> **출력 포맷의 강제성 (순수 HTML)**
> 생성된 초안은 곧바로 그룹웨어 에디터에 붙여넣기(Ctrl+V) 되어야 하므로 마크다운 백틱(```html)이나 `<html>`, `<body>` 같은 외부 뼈대 태그가 섞이면 레이아웃이 깨집니다. 프롬프트 내에 **"순수 HTML 본문 태그만 반환할 것"**이라는 조건을 반드시 기재해야 합니다.

---

## 5. 기타 중요 사항

- **엑셀/표 데이터 핸들링:**
  기본적으로 LLM(Gemini 등)은 복잡한 엑셀 파일(특히 병합된 셀이 많고 중간에 빈 열이 많은 견적서 양식)을 직접 읽을 때 열이 뒤틀리며 데이터를 누락하는 치명적 오류가 발생합니다. 따라서 `pandas.dropna()`를 이용해 비어있는 행렬 노이즈를 제거하고 **마크다운 표(Markdown)**로 변환하여 주입한 현재의 해결 방식(Method 2)을 유지하거나 더욱 고도화해야 합니다.
- **RAG(검색 증강 생성) 품질 향상:** 
  현재는 단순 Keyword 기반이 아닌 Reranker를 도입하여 정확도를 획기적으로 올렸습니다. 추후 문서량이 방대해질 경우 벡터 DB 연동을 통한 하이브리드 검색(유사도+키워드)을 고려해 볼 수 있습니다.
- **보안 및 환경설정 격리 (`.env` & `config.py`):**
  사내 DB 관리자 계정/비밀번호와 AI API Key의 외부 유출을 방지하기 위해 `python-dotenv` 기반의 `.env` 분리 구조가 적용되어 있습니다. 인수인계 전달 시에는 `.env.example` 템플릿 형태로 전달하고 실제 비밀번호는 별도 보안 경로로 안내해야 합니다.
