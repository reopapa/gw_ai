# AI 기안/품의서 초안 생성기 - 향후 개선 및 업그레이드 항목 (Future Improvements)

이 문서는 프로젝트의 완성도와 엔터프라이즈급 안정성을 확보하기 위해 향후 개선해야 할 항목들을 추적하고 관리하는 문서입니다. 각 항목이 해결될 때마다 상태를 `[x]` (처리 완료)로 변경합니다.

---

## 1. UX/UI 및 체감 속도 개선
- `[ ]` **초안 생성 실시간 스트리밍(Streaming):** 4단계 초안 생성 시, 완성될 때까지 기다리지 않고 실시간으로 글자가 타이핑되듯 화면에 출력되도록 하여 체감 대기시간 최소화
- `[ ]` **커스텀 테마(브랜드 컬러) 적용:** 실제 연동될 그룹웨어의 브랜드 테마(버튼 색상, 폰트, 배경색 등)를 `.streamlit/config.toml`에 세팅하여 이질감 해소

## 2. 안정성 및 예외 처리 (견고함 확보)
- `[ ]` **API Rate Limit 방어 로직 (재시도 로직):** 구글 Gemini API 통신 시 발생하는 429 에러(속도 제한 등) 방어를 위해, 실패 시 지수 백오프(Exponential Backoff) 기반의 자동 재시도 로직(`tenacity` 등) 적용
- `[ ]` **임시 찌꺼기 파일 완벽 청소 (Garbage Collection):** 비정상 종료 시에도 업로드된 임시 폴더(`uploads/`)의 찌꺼기 파일들이 주기적으로 삭제되도록 안전망 로직 추가

## 3. RAG(과거 문서 검색) 정확도 고도화
- `[ ]` **하이브리드 검색 도입:** 단순 키워드/텍스트 매칭과 Reranker를 넘어, 과거 기안서의 내용을 벡터(Vector) 임베딩 처리하여 문맥적 의미 기반으로 가장 적합한 문서를 찾는 하이브리드 서치 파이프라인 구축

## 4. 보안 및 컴플라이언스 (엔터프라이즈 필수)
- `[ ]` **민감 정보(PII) 로컬 마스킹:** 첨부파일이나 과거 결재 문서에 포함된 계좌번호, 주민등록번호, 연락처 등 민감한 개인정보를 정규식(Regex)을 이용해 로컬에서 미리 `***` 처리한 뒤에 LLM으로 전송하는 보안 전처리 파이프라인 추가

## 5. 기타 기능 보강
- `[x]` **채팅 기반 부분 수정 고도화:** 4단계에서 초안 전체를 뭉뚱그려 수정하는 것이 아니라, 사용자가 특정 단락을 지정하거나, 부분만 재생성(Regenerate)할 수 있는 UI/UX 구현 완료 (HTML 문단 기준 분할)

## 6. 데이터 관리 및 운영 로직 (추가 예정)
- `[ ]` **최근 3개년치 데이터 초기 적재 (Batch):** 당해 연도 포함, 최근 3개년치 과거 결재 문서를 `gw_ai.py`를 통해 전면(Full) 스캔하여 AI 전용 DB에 적재하는 대규모 초기화 배치 작업 준비 (기간 분할 실행 및 예외 처리 고도화 필요)
