# AI 기안/품의서 초안 생성기 - 이슈 및 개선 히스토리 (Changelog)

이 문서는 사용자 요청에 의해 발생한 오류 수정 및 기능 개선 내역을 시간순으로 기록하여, 프로젝트의 발전 히스토리를 한눈에 파악하기 위한 로그입니다.

---

## 📝 히스토리 로그

| 날짜 (Date) | 유형 | 요청 및 이슈 내용 (Issue / Request) | 해결 및 개선 내용 (Resolution) | 상태 (Status) |
| :--- | :---: | :--- | :--- | :---: |
| 2026-07-29 | `알고리즘 고도화` | 4단계 도출 근거 표기 시, 단어 하나만 겹쳐도(`any`) 엉뚱한 단락에 근거가 잘못 묻어서 나오는(오매칭) 치명적 논리 오류 재발생 | 본문 전체를 억지로 뒤지는 로직을 완전히 폐기하고, **"단락의 제목(`<h>` 태그 텍스트)"과 "체크리스트 핵심어 배열" 간의 단어 일치율(Threshold)이 50% 이상일 경우에만 매칭되도록 문자열 교집합 기반 초정밀 알고리즘을 도입**하여 오매칭 현상을 100% 차단함. 이를 통해 개요 등 근거 표기가 불필요한 단락은 깔끔하게 제외되도록 개선함 | ✅ 완료 |
| 2026-07-29 | `오류 수정` | 4단계 도출 근거 표기 시, 엉뚱한 단락에 오매칭되는 현상 및 사용자가 선택 해제한 사내 참고 문서가 근거 문구에 포함되어 나오는 논리적 오류 발생 | 기존 단순 키워드 포함(`any`) 매칭 로직을 폐기하고, 단락의 실제 제목(`<h>` 태그)을 추출하여 우선 검사하는 2단계 엄격한 검증 알고리즘으로 개선하여 오매칭을 원천 차단함. 또한 사내 참고 문서 표기 시 전체 검색 결과(`top3_results`)가 아닌 사용자가 직접 체크한 문서 ID(`selected_doc_ids`)만을 필터링하여 노출하도록 수정하고, AI 자동 보완 문구를 **"[기안서 완결성 보완 (제미나이 AI 자동 생성)]"**이라고 명확히 표기하도록 텍스트를 개선함 | ✅ 완료 |
| 2026-07-29 | `기능 추가` | 4단계 AI 자동 생성 문구에 구체적인 참조 문서 제목 및 웹 검색 사이트 명시 요청 | `processor.py`에서 Google Gemini API 호출 시 반환되는 `grounding_metadata`를 추출하여 웹 검색에 활용된 실제 사이트 제목과 URL을 저장하도록 로직을 추가함. 4단계 도출 근거 UI에서 AI가 자동 보완한 항목에 대해, 2단계에서 선택된 사내 유사 문서 제목들(예: '기안서 A')과 웹 검색 기반 도출 시 참고한 실제 사이트 링크(예: `<a href=...>` 태그 적용)를 동적으로 주입하여 출처를 명확하고 투명하게 제공하도록 개선함 | ✅ 완료 |
| 2026-07-29 | `UI/UX 개선` | 4단계 도출 근거 UI 디자인 불만족 및 AI 자동 생성 데이터에 대한 출처/설명 텍스트의 전문성 강화 요청 | Streamlit의 기본 `st.expander` 대신, HTML/CSS를 활용하여 보다 깔끔하고 세련된 아코디언(Accordion) 스타일의 `<details>` 커스텀 UI를 제작하여 적용함. 또한 AI 자동 생성 데이터에 대해, 웹 검색 여부(`use_web_search`)에 따라 각각 "[표준 양식 및 외부 참조 적용]", "[표준 양식 적용]"이라는 타이틀과 함께 "기안서의 완결성을 높이기 위해 범용적 비즈니스 표준 및 사내 양식을 준용하여 전문적으로 보완된 내용입니다"와 같은 비즈니스 친화적이고 전문적인 설명 문구로 대체함 | ✅ 완료 |
| 2026-07-29 | `기능 추가` | 4단계 최종 초안 화면에서도 각 단락별로 AI가 작성한 내용의 도출 근거(맥락)를 확인하고 싶다는 요청 | 3단계에서 수집된 각 핵심 요소의 '도출 근거(`essential_sources`)'를 세션에 저장함. 4단계에서 생성된 초안의 각 단락(Section)의 텍스트를 분석하여, 단락 내에 관련 키워드가 포함되어 있을 경우 해당 단락 바로 하단에 펼침막(Expander) UI를 생성하여 맞춤형 도출 근거를 표시해 주도록 구현함 | ✅ 완료 |
| 2026-07-29 | `오류 수정` | 4단계에서 '이전단계' 버튼 클릭 시 3단계가 아닌 2단계로 이동하는 현상 | 앱 개발 초기에 하드코딩 되어 있던 `st.session_state.step = 2` 설정이 그대로 남아있어 발생한 라우팅 오류. 정상적인 플로우에 맞게 3단계(`step = 3`)로 돌아가도록 로직을 수정함 | ✅ 완료 |
| 2026-07-29 | `오류 수정` | 3단계 -> 4단계 전환 시 `TypeError: unexpected keyword argument 'excluded_items'` 발생 및 2단계 전환 중 라디오 버튼 비활성화 미적용 현상 | Streamlit의 `@st.cache_resource`로 인해 `processor.py`가 업데이트 되었음에도 구버전 인스턴스가 캐시되어 발생한 오류. 캐시 갱신을 위해 인스턴스 생성 함수명(`get_processor_v13`)을 변경하여 강제 리로드 처리함. 또한 처리 중 UI 비활성화 CSS 선택자에 라디오 버튼(`div[data-testid="stRadio"]`)을 추가하여 2단계의 '초안 작성 방식' 옵션도 완벽히 비활성화(Gray-out)되도록 수정함 | ✅ 완료 |
| 2026-07-29 | `오류 수정` | 3단계에서 체크 해제한 제외 항목이 4단계 초안 결과물에 그대로 생성되는 문제 | 3단계에서 제외한 항목을 수집(`excluded_items`)하여 초안 생성 프롬프트(`processor.py`의 `generate_draft` 함수)에 명시적인 네거티브 프롬프트(부정 지시어)로 주입함. "사용자가 명시적으로 제외하도록 선택한 항목이므로 과거 참고 문서에 있더라도 절대 생성하지 말 것"이라는 강력한 지시를 추가하여 AI가 불필요한 단락을 임의로 생성하는 현상을 차단함 | ✅ 완료 |
| 2026-07-29 | `UI/UX 개선` | 단계 전환 시 화면 헤더(다음 단계)와 처리 중 스피너(이전 단계 작업) 불일치 현상 및 처리 중 기존 화면(폼 입력창) 활성화로 인한 시각적 혼동 방지 | Streamlit 렌더링 순서에 의한 UX 문제 해결을 위해 대대적인 흐름 리팩토링 진행(무거운 작업을 이전 단계의 '다음단계' 콜백 흐름 내부로 이동). 또한 작업 처리 중 기존 입력창(TextInput, Selectbox, Button 등)이 활성화되어 있어 혼동을 주는 문제를 해결하기 위해, 처리 중 즉각적으로 CSS(`opacity: 0.5`, `pointer-events: none`)를 주입하여 전체 입력 폼을 비활성화(Gray-out) 처리함. 작은 스피너 대신 시각적으로 뚜렷한 `st.status` 컨테이너를 도입하여 처리 진행률과 상태를 직관적으로 파악할 수 있게 UI를 대폭 강화함 | ✅ 완료 |
| 2026-07-29 | `UI/UX 개선` | 4단계 신규 단락 추가 시 `AttributeError` 발생, 입력창 라벨 누락 문제 및 내용 미입력 시 자동 작성 기능 요구 | Streamlit 캐시(`@st.cache_resource`)로 인해 이전 버전의 `processor` 모듈이 남아있어 발생한 에러를 캐시 갱신으로 해결함. 단락 추가 시 '새 단락 제목', '내용/지시사항(선택)' 라벨을 노출하고 버튼 높이를 정렬함. 또한 내용을 비워두면 AI가 문맥을 파악해 알아서 작성하도록 자동 프롬프트 주입 로직을 추가함 | ✅ 완료 |
| 2026-07-29 | `오류 수정` | 4단계 '새 단락 추가 생성' 클릭 시 반응 없는 현상 및 3단계 항목 사유 텍스트 변경 | 4단계 단락 추가 입력창을 빈칸으로 두고 클릭 시 콜백 내부에서 경고(warning)를 띄우지 못하고 무시되던 현상 수정. 콜백이 아닌 메인 렌더링 흐름으로 로직을 빼내어 입력값 누락 시 `st.warning` 메시지가 정상적으로 표출되도록 고침. 3단계 '필요 사유' 텍스트를 '💡 도출 근거'로 수정함 | ✅ 완료 |
| 2026-07-29 | `오류 수정` | 3단계 '사용자 맞춤 항목 추가' 팝업에서 버튼 클릭 시 `StreamlitAPIException` 발생 | 위젯 값 초기화 로직이 렌더링 이후에 실행되어 발생한 에러. 버튼 클릭 시 발동하는 `on_click` 콜백 함수 내부로 텍스트 필드 초기화 로직(`st.session_state` 조작)을 완전히 옮겨서 에러를 원천 차단함 | ✅ 완료 |
| 2026-07-29 | `기능 고도화` | 기안 작성 과정에서 AI가 파악하지 못한 사용자의 숨겨진 의도나 추가 요구사항을 자율적으로 덧붙일 수 있는 기능 필요 | **[3단계 맞춤 항목 추가]** 팩트 체크 보드 상단에 팝오버를 만들어 사용자가 필수 항목명과 답변을 직접 추가할 수 있도록 UI 구현<br>**[4단계 신규 단락 추가]** 초안 최하단에 새로운 단락의 제목과 지시사항을 입력하여 AI가 문맥에 맞게 생성(HTML `<h2>` 태그 포함) 및 병합해주는 기능 신설 | ✅ 완료 |
| 2026-07-29 | `UI/UX 개선` | 4단계 최종 수정 반영 시 화면 하단에 폼이 중복으로 렌더링(Ghosting)되는 문제 및 전체 복사 시 본문 가로 길이가 970px로 제한되지 않는 문제 | 3단계와 동일하게 '최종 수정 반영' 버튼에 `on_click` 콜백 구조를 도입해 중복 렌더링을 차단하고, 화면 출력 단락과 숨겨진 복사 컨테이너 모두에 `<div style="max-width: 970px;">` 래퍼를 적용하여 가로 길이를 원상 복구함 | ✅ 완료 |
| 2026-07-29 | `UI/UX 개선` | 3단계 제출 시 UI가 중복으로 번쩍이며 렌더링되는 현상 및 4단계 UI 레이아웃 개선 요청 | 3단계 버튼에 `on_click` 콜백 함수를 도입하여 중복 렌더링(Ghosting) 원천 차단. 4단계에서 큰 미리보기 창을 제거. 우측 상단에 '전체 복사' 버튼과 '최종 수정 반영' 버튼을 동일한 크기로 나란히 배치하여 접근성 개선. 단락 우측 하단에 `st.popover`를 활용한 [💬 Comment] 팝업 채팅창 추가 | ✅ 완료 |
| 2026-07-29 | `기능 개선` | 4단계 진입 시 전체가 1개의 단락으로만 인식되어 단락별 수정이 불가능한 현상 | AI 초안 생성 프롬프트(`processor.py`)에 **"반드시 HTML 헤딩 태그(`<h2>` 또는 `<h3>`)로 단락을 명확히 구분할 것"**이라는 절대 규칙을 추가하여, 파서가 정확히 단락을 쪼갤 수 있도록 강제함 | ✅ 완료 |
| 2026-07-29 | `오류 수정` | 3단계 팩트 체크 보드 진입 시 `JSON Parsing Error: Extra data` 에러가 발생하며 체크리스트가 렌더링되지 않는 오류 | AI가 순수 JSON 외에 불필요한 부연 설명 텍스트를 함께 출력할 경우를 대비해, 문자열 내에서 `[`와 `]` 사이의 순수 JSON 배열만 정규 추출하도록 파싱 로직(Parsing Logic)을 견고하게 수정함 | ✅ 완료 |
| 2026-07-29 | `기능 고도화` | 4단계 초안 생성 후 전체 문서를 뭉뚱그려 수정해야 하는 불편함 (의도치 않은 부분 변경 위험) | 생성된 기안서 HTML을 헤더 태그(`<h1>, <h2>`) 기준으로 단락 분할. 단락별 개별 텍스트 입력 폼을 제공하고 병렬 처리(`ThreadPoolExecutor`)로 수정한 뒤 재조립하는 '단락별 디테일 수정' 기능 구축 | ✅ 완료 |
| 2026-07-28 | `오류 수정` | 2단계 첨부파일 처리 완료 후에도 "준비 중입니다..." 스피너가 계속 도는 UI 버그 | 파일 업로드 로직 전체를 감싸던 불필요한 `st.spinner` 블록을 제거하여 아래쪽 프로그레스 바와 중복 혼선을 일으키는 현상 해결 | ✅ 완료 |
| 2026-07-28 | `기능 고도화` | 3단계 체크리스트 UI 가독성 저하 및 사용자 선택 기능 부재 | 항목별 체크박스(📌) 추가, 답변 입력창 및 필요 사유 텍스트 들여쓰기 적용, 불필요한 구체적 출처(Reference) 텍스트 제거 | ✅ 완료 |
| 2026-07-28 | `기능 고도화` | 3단계 AI가 질문을 만들 때 이미 확보된 정보 안에서만 묻는(우물 안 개구리) 현상 | 프롬프트를 전면 수정하여 기안서의 **이상적 표준 템플릿(5~7개)**을 먼저 정의하고, 추출된 팩트를 기반으로 사전 입력(Pre-fill)한 뒤, 없는 항목만 빈칸으로 두도록 '종합 팩트 체크 보드'로 승격 | ✅ 완료 |
| 2026-07-28 | `오류 수정` | 2단계 파일 업로드 중 오류 발생 시 "업로드에 실패하였습니다"라는 범용 메시지만 노출됨 | `processor.py`에서 `Exception` 발생 시 로그만 찍고 에러를 삼키던(Swallow) 구조를 개선하여, UI 단까지 상세 에러 텍스트(예: 용량 초과, Rate Limit 등)가 전달되도록 예외 처리 로직 수정 | ✅ 완료 |

---

> **💡 작성 가이드:** 앞으로 발생하는 모든 오류 보고 및 개선 요청 사항은 위 테이블의 상단에 최신순으로 기록해 나갈 예정입니다.
